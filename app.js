/*
Utiliser axios pour récuperer 
le nom d'une ville quand un utilisateur saisi le code postal 
*/

var vm = new Vue({
    el: "#app",
    data: { postalCode= 0 },
    watch: {
        
    }
});
